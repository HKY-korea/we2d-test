const engNameToKor = (name) => {
  if (name === "hardCase") return "하드케이스";
  if (name === "jellyCase") return "젤리케이스";
  if (name === "apple") return "아이폰";
  if (name === "samsung") return "삼성";
}

export default engNameToKor;